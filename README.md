#### Ansible alfresco role example.
Configured for external authentication, separate SOLR installation and existed data initialization (For alfresco upgrade).

Keycloak is used as external OAUTH 2.0 server